import 'package:flutter/material.dart';

class UpcomingHabitsCard extends StatelessWidget {
  const UpcomingHabitsCard({super.key});

  
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}